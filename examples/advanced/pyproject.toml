[project]
name = "vectorless-advanced-example"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "vectorless",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
